name: CI Build

on: [push]

jobs:
  build:
    runs-on: ultraman-k8s-self-runner # Use your self-hosted runner
    steps:
    - uses: actions/checkout@v2
    - name: Run a script
      run: |
        echo "Hello, world!"
        echo "This runs on a self-hosted runner."

